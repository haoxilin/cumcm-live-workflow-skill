# 最小工作示例（Worked Example）——水箱注水问题

> 用途：展示本技能"从题面到可验证结果"的完整链路。这是一个**通用教学案例**（不涉及任何真实赛题数据），让 Agent 在正式工作前有一个"对的样子"可参照。
> 重点不是题目本身，而是**每一步怎么做、怎么验证、怎么呈现**。

## 题目（示意）

一个截面积 $A=2\,\mathrm{m^2}$ 的水箱，底部有泄流口。以恒定流量 $Q_{in}=0.05\,\mathrm{m^3/s}$ 注水，泄流口出流按孔口公式 $Q_{out}=k\sqrt{h}$ 排出（$k=0.02\,\mathrm{m^{5/2}/s}$）。初始水位 $h(0)=0$。求：
1. 水位 $h(t)$ 随时间的演化（0~1200 s）；
2. 达到 $h=4\,\mathrm{m}$ 的时刻；
3. 判断 $k$、$Q_{in}$ 波动 $\pm5\%$ 对到达时刻的影响。

## 第 1 步：题意红线与解析自洽（对应技能 §1）

- **题干明写的物理事实必须建模**：质量守恒 → $\dfrac{d}{dt}(A h)=Q_{in}-Q_{out}=Q_{in}-k\sqrt{h}$。
- **单位自洽检查**：$k\sqrt{h}$ 的量纲 = $\mathrm{m^{5/2}/s}\times\sqrt{\mathrm m}=\mathrm{m^3/s}$ ✓，与 $Q$ 一致。
- **解析预判**（不依赖数值）：
  - 平衡水位 $h^*$：$Q_{in}=k\sqrt{h^*} \Rightarrow h^*=(Q_{in}/k)^2=(0.05/0.02)^2=6.25\,\mathrm{m}$；
  - 量纲/极限检查：$t\to\infty$ 时 $h\to h^*$ 且不超过 $h^*$（物理合理）。

## 第 2 步：数值求解（对应技能 §2 数值严谨性红线）

- 用 `scipy.integrate.solve_ivp`（RK45）求解 $h'=(Q_{in}-k\sqrt{h})/A$，$h(0)=0$；
- **步长收敛检查**：把容许误差从 $10^{-4}$ 收紧到 $10^{-8}$，$h(t)$ 变化 < $10^{-6}$ 量级才认可；
- 达 4 m 时刻：二分/插值定位 $t(h=4)\approx 190.2\,\mathrm{s}$（示例值，实际以代码输出为准）。

## 第 3 步：交叉验证（对应技能 §2 自建基准）

- **解析特例**：把 $Q_{out}$ 关掉（$k=0$）→ 线性注水 $h=Q_{in}t/A$，数值解必须精确复现（误差 < 1e-10）；
- **长期行为**：数值解 $t\to\infty$ 应趋近解析平衡水位 $h^*=6.25\,\mathrm{m}$；
- **守恒检验**：$\int_0^T (Q_{in}-Q_{out})dt = A[h(T)-h(0)]$ 残差 < 1e-9。
  三关全过，数值才可信。

## 第 4 步：结果呈现（对应技能 §3 结构模板 + 国奖指标）

- 结果表格（三线表）：0/300/600/900/1200 s 的水位与误差；
- 一张曲线图（中文标签、SimHei、含单位、网格）；
- 每张图后跟一段分析文字，说明"该图说明什么"。

## 第 5 步：敏感性分析（对应技能 §2/§8 实跑红线）

- 对 $k$ 和 $Q_{in}$ 各做 $\pm5\%$ 扰动，用 `references/sensitivity_framework.py` 跑：
  - $Q_{in}$ 是该系统的**正向驱动**，$\pm5\%$ 对到达时刻影响约 $\mp 5\sim10\%$（超线性，因 $\sqrt{h}$ 耦合）；
  - $k$ 负向扰动（泄流变小）使到达更快，正向相反——**响应不对称**，如实呈现。
- 结论必须带数值，禁止写"较稳健"三个字了事。

## 第 6 步：结构化交付（对应技能 §3 成品结构模板）

成品按结构模板组织（摘要五段式 / 1 问题重述与分析 / 2 假设与符号 / 3 建模求解 / 4 检验 / 5 评价），每个问题章内固定"模型建立/模型求解/结果与分析"三小节，编号用 LaTeX 自动编号，列表用 (i)(ii)。

---

## 这个示例说明什么

| 环节 | 示例中对应 | 反例（常见翻车） |
|---|---|---|
| 题意红线 | 质量守恒 + 单位自洽 | 直接抄网上水箱模型不管 $k$ 量纲 |
| 解析验证 | $h^*=(Q/k)^2$ 解析预判 | 只跑数值不验证，平衡水位算错也不知道 |
| 交叉验证 | 关出流→线性解复现 | 单次数值结果直接当答案 |
| 敏感性 | 实跑 $\pm5\%$ + 不对称解释 | 编"结论较稳健" |
| 结构 | 完整小节+编号 | 无小标题、编号混乱（v5.2 已修复此坑） |

> 真正打比赛时，把"水箱"换成你的真实题面，把"质量守恒/孔口公式"换成你的模型与推导，其余环节一字不改地照做即可。
